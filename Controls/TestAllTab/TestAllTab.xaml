<UserControl x:Class="Controls.TestAllTab"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="Auto" Width="Auto">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,5">
            <TextBlock Text="Folder:" VerticalAlignment="Center"/>
            <ComboBox x:Name="FolderComboBox" Width="200" Margin="5,0" SelectionChanged="FolderComboBox_SelectionChanged"/>
            <Button x:Name="RefreshButton" Content="Refresh" Click="RefreshButton_Click" Margin="5,0"/>
        </StackPanel>
        <ListBox x:Name="FileList" Grid.Row="1"/>
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,5,0,0">
            <Button x:Name="TestAllButton" Content="TestAll" Width="100" Click="TestAllButton_Click"/>
            <TextBlock x:Name="ResultText" Margin="10,3,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
